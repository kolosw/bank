import { BankAccountCurrency } from './bank-account-currency';

describe('BankAccountCurrency', () => {
  it('should create an instance', () => {
    expect(new BankAccountCurrency()).toBeTruthy();
  });
});
